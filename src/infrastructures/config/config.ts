const HOST='http://localhost';
const PORT= 4000;


const config:any  = {
    host: HOST,
    port: PORT,
    hostname:`${HOST}:${PORT}/`,
    db: {
        host: ``,
        database: '',
        user: '',
        password: '',
        port: 0
    },
}

export default config;
